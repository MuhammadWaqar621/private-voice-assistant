services:
  backend:
    build: ./backend
    ports:
      - "8010:8010"
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://localhost:8010
    ports:
      - "4180:4180"
    depends_on:
      - backend
